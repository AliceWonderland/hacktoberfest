// LANGUAGE: Javascript
// ENV: Node.js
// AUTHOR: Jonas Karg
// GITHUB: https://github.com/KargJonas

// This code is self-documenting  :D
console.log( ( () => {
  let _$ = 111, $_ = Math.floor( Math.PI ), _$_ = $$ => String.fromCharCode( $$ ), $_$ = $ => $.join( String() )
  return $_$( [Math.floor( _$ / 2 ) + ( $_ - 1 ) * 11 - 5, _$ - 20 / 5 * 2.5, _$ - $_, _$ - $_, _$, 32, 87, _$, _$ + $_, _$ - $_, _$ - 11, ( _$ - Math.pow( 10, 2 ) ) * 3].map( $ => _$_( $ ) ) )
} )() );
